# Переменные окружения для CRM-системы автосервиса по электрике
# Скопируйте этот файл в .env и заполните свои значения

# Порт сервера
PORT=3000

# Настройки базы данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=electric_service_crm

# Настройки JWT
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=7d

# Настройки для Render (если развертывается на Render)
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com
RENDER_EXTERNAL_HOSTNAME=your-app-name.onrender.com

# Строка подключения к базе данных для Render
DATABASE_URL=postgresql://username:password@hostname:port/database_name