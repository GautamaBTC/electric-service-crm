# Переменные окружения для CRM-системы автосервиса по электрике
# Скопируйте этот файл в .env и заполните свои значения

# Основные настройки сервера
# Окружение: development (для локальной разработки) или production (для продакшена)
NODE_ENV=development

# Порт сервера (для локальной разработки)
PORT=3000

# Настройки базы данных PostgreSQL (для локальной разработки)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=electric_service_crm

# Настройки JWT (JSON Web Token)
# Секретный ключ для подписи JWT токенов
# ВНИМАНИЕ: В продакшене используйте длинную и сложную строку
JWT_SECRET=your_super_secret_jwt_key_here

# Срок действия JWT токена (например: 7d, 1h, 30m)
JWT_EXPIRES_IN=7d

# Настройки для развертывания на Render
# Внешний URL приложения (для Render)
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com

# Имя хоста (для Render)
RENDER_EXTERNAL_HOSTNAME=your-app-name.onrender.com

# Строка подключения к базе данных для Render
# Формат: postgresql://username:password@hostname:port/database_name
# Эта переменная будет автоматически установлена Render при подключении базы данных
DATABASE_URL=postgresql://username:password@hostname:port/database_name